<?xml version="1.0" encoding="utf-8"?>
<!-- The wagon from icon_wagon.pix (see app/src/main/assets/art), rasterised once at a
     fixed size with hard nearest-neighbour edges — the same wagon the supplies readout
     uses in-game, rather than art invented just for the launcher. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/ic_launcher_wagon"
    android:gravity="center" />
